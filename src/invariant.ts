/**
 * dsh-minigames invariant companion. No runtime invariant: the node half has
 * no cross-plugin mutable state to assert — the entire product surface is the
 * browser-side game panel, whose own contract (game registry discipline) is
 * exercised by the registry unit tests rather than by a node-side event or
 * data relationship.
 */

export {}
